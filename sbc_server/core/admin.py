"""
Django-generated file
Registers viewsets to be editable on Django's admin interface.

"""

from django.contrib import admin
from .models import Arm, Session

admin.site.register(Arm)
admin.site.register(Session)
